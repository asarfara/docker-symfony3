version: '2'
networks:
    symfony:
        driver: 'bridge'

services:
  php:
      build:
          context: .
          dockerfile: .docker/php/Dockerfile
      container_name: php
      networks:
          - symfony
  web:
      build:
          context: .
          dockerfile: .docker/web/Dockerfile
      container_name: web
      ports:
          - "80:80"
      links:
          - php
      networks:
          - symfony